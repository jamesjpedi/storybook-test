import { Meta } from '@storybook/addon-docs/blocks';

import { ChangelogViewer } from './changelog/ChangelogViewer';

<Meta title="Changelog" />

# Changelog

Pick a version to load its notes. Each version is a separate markdown file in `src/docs/changelog/versions/`. Add a new `.md` file there whenever you want another entry — any filename works, semantic versioning is not required.

<ChangelogViewer />
